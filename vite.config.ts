import { defineConfig } from "vite";

// Served from https://golamrabbani99.github.io/portfolio/ (GitHub Pages project
// site), so assets must be referenced under the /portfolio/ sub-path.
export default defineConfig({
  base: "/portfolio/",
});
